version: '3.3'

services:
  web:
    build: .
    ports:
     - "5000:5000"
    volumes:
      - type: volume
    source: cache
    target: /code
    volume:
      nocopy: true
  redis:
    image: "redis:alpine"
